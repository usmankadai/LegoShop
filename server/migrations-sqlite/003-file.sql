CREATE TABLE upload (
    legoId varchar(20) PRIMARY KEY,
    legoName text,
    instructions text,
    file text
);


-- INSERT INTO upload (legoId, legoName, instructions);

-- down?

drop table upload;
